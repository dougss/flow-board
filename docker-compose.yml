services:
  flow-board:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    volumes:
      - flow-board-data:/app/prisma
    environment:
      DATABASE_URL: file:./prisma/dev.db
    restart: unless-stopped

volumes:
  flow-board-data:
